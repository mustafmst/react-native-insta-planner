export async function getInstagramPage(nickname: string) {
  // TODO: get instagram images
  //   let page = await fetch(`https://www.instagram.com/${nickname}/`);
  //   console.log(page.body);
  console.log(
    `get instagram images from https://www.instagram.com/${nickname}/`
  );
}
